namespace Schemata.Scheduling.Skeleton;

/// <summary>Constants used by the Scheduling domain.</summary>
public static class SchedulingConstants
{
    /// <summary>Well-known keys for Scheduling request-handler registrations.</summary>
    public static class Handlers
    {
        /// <summary>The Foundation handler used when no optional wrapper replaces the unkeyed alias.</summary>
        public const string Default = "default";
    }
}
