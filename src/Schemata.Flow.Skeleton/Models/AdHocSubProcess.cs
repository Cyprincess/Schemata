namespace Schemata.Flow.Skeleton.Models;

/// <summary>
///     A BPMN Ad-Hoc Sub-Process — a sub-process whose activities have no
///     predefined sequence; they are performed in any order at the performer's discretion.
/// </summary>
public sealed class AdHocSubProcess : SubProcess;
